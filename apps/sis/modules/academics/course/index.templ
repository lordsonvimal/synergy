package course

import "github.com/lordsonvimal/synergy/apps/sis/modules/shared/ui/layouts"

templ RenderIndexPage(courses []CourseInfo) {
	@layouts.Layout(layouts.LayoutConfig{Title: "Courses"}) {
		<h1 class="text-2xl font-bold mb-6">Courses</h1>
		@RenderCoursesList(courses)
	}
}
