# Gin server mode: set to "release" for production (enables optimised logging and disables debug output).
# Leave empty or set to "debug" for local development.
GIN_MODE=

# Directory where per-game write-ahead log (.wal) files are stored.
# Defaults to the current working directory when not set.
DATA_DIR=./data
