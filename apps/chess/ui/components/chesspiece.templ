package components

import (
	"github.com/lordsonvimal/synergy/apps/chess/engine"
	"github.com/lordsonvimal/synergy/apps/chess/ui/helpers"
)

templ RenderChessPiece(color engine.Color, piece engine.Piece) {
	{{
		label := helpers.ColorName(color) + " " + helpers.PieceName(piece)
		pieceClass := "chess-piece-white"
		if color == engine.Black {
			pieceClass = "chess-piece-black"
		}
	}}
	<span aria-label={ label } role="img" class={ pieceClass }>
		if color == engine.White {
			switch piece {
				case engine.Pawn:
					♙
				case engine.Knight:
					♘
				case engine.Bishop:
					♗
				case engine.Rook:
					♖
				case engine.Queen:
					♕
				case engine.King:
					♔
			}
		} else {
			switch piece {
				case engine.Pawn:
					♟
				case engine.Knight:
					♞
				case engine.Bishop:
					♝
				case engine.Rook:
					♜
				case engine.Queen:
					♛
				case engine.King:
					♚
			}
		}
	</span>
}
